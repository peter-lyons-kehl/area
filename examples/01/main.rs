mod idx {
    use area::refs_alternatives::refs_idx as refs;

    #[path = "../types.rs"]
    mod types;
}

mod bin {
    use area::refs_alternatives::refs_bin as refs;

    #[path = "../types.rs"]
    mod types;
}

fn main() {}
