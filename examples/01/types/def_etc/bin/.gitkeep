Keep this directory, even though there is no source code here. Otherwise ../../types.rs -> mod bin
-> mod def will not resolve.

This file is necessary, because GIT doesn't allow to add/track empty directories.